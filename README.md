# Directed-Graph-Acyclicity-Checker-Sink-Elimination-Algorithm-
This project is a directed graph analysis system developed for the Algorithms coursework to determine whether a graph is acyclic (contains no cycles). It uses the Sink Elimination Algorithm, where vertices with no outgoing edges are repeatedly removed. If all vertices are eliminated, the graph is acyclic; otherwise, a cycle exists.
